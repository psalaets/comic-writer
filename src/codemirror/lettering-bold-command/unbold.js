
/*
  split operation:

  given bold token
  range to unbold

  return array of tokens with everything set correctly

  cases:

  unbolding left side
  unbolding right side
  unbolding in middle
  unbolding entire thing
*/
export function unbold(boldToken, unboldStart, unboldEnd) {

}
