image: node:10

cache:
  paths:
    - node_modules/

before_script:
  - npm install

stages:
  - test

run_tests:
  stage: test
  script:
    - npm ci
    - CI=true npm test

pages:
  stage: deploy
  script:
    - CI=true npm run build
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
