image: node:10

cache:
  paths:
    - node_modules/

before_script:
  - npm install

stages:
  - test
  - deploy

run_tests:
  stage: test
  script:
    - npm ci
    - CI=true npm test

pages:
  stage: deploy
  script:
    - CI=true npm run build
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
